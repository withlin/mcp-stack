{{- if .Values.victoriaMetricsMcp.enabled -}}
{{- $component := "victoriametrics-mcp" -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "mcp-stack.componentFullname" (dict "context" . "component" $component) }}
  labels:
    {{- include "mcp-stack.componentLabels" (dict "context" . "component" $component) | nindent 4 }}
spec:
  replicas: {{ .Values.victoriaMetricsMcp.replicaCount }}
  selector:
    matchLabels:
      {{- include "mcp-stack.componentSelectorLabels" (dict "context" . "component" $component) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "mcp-stack.componentSelectorLabels" (dict "context" . "component" $component) | nindent 8 }}
      {{- with .Values.victoriaMetricsMcp.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      {{- with .Values.victoriaMetricsMcp.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: victoriametrics-mcp
          {{ $image := .Values.victoriaMetricsMcp.image.repository }}
          {{ $tag := .Values.victoriaMetricsMcp.image.tag }}
          image: "{{ if $tag }}{{ printf "%s:%s" $image $tag }}{{ else }}{{ $image }}{{ end }}"
          imagePullPolicy: {{ .Values.victoriaMetricsMcp.image.pullPolicy }}
          {{- with .Values.victoriaMetricsMcp.args }}
          args:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.victoriaMetricsMcp.service.port }}
              protocol: TCP
          {{- $root := . -}}
          {{- $envList := list -}}
          {{- if not (or $root.Values.victoriaMetricsMcp.secret.create $root.Values.victoriaMetricsMcp.secret.name) }}
          {{- range $key := sortAlpha (keys $root.Values.victoriaMetricsMcp.env) }}
          {{- $value := index $root.Values.victoriaMetricsMcp.env $key -}}
          {{- if $value }}
          {{- $envList = append $envList (dict "name" $key "value" (printf "%v" $value)) -}}
          {{- end }}
          {{- end }}
          {{- end }}
          {{- range .Values.victoriaMetricsMcp.extraEnv }}
          {{- $envList = append $envList . -}}
          {{- end }}
          {{- if gt (len $envList) 0 }}
          env:
            {{- toYaml $envList | nindent 12 }}
          {{- end }}
          {{- $envFrom := list -}}
          {{- if or .Values.victoriaMetricsMcp.secret.create .Values.victoriaMetricsMcp.secret.name }}
          {{- $secretName := include "mcp-stack.componentSecretName" (dict "context" . "component" $component "secret" .Values.victoriaMetricsMcp.secret) -}}
          {{- $envFrom = append $envFrom (dict "secretRef" (dict "name" $secretName)) -}}
          {{- end }}
          {{- range .Values.victoriaMetricsMcp.envFrom }}
          {{- $envFrom = append $envFrom . -}}
          {{- end }}
          {{- if gt (len $envFrom) 0 }}
          envFrom:
            {{- toYaml $envFrom | nindent 12 }}
          {{- end }}
          {{- with .Values.victoriaMetricsMcp.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.victoriaMetricsMcp.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.victoriaMetricsMcp.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.victoriaMetricsMcp.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .Values.victoriaMetricsMcp.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.victoriaMetricsMcp.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.victoriaMetricsMcp.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
